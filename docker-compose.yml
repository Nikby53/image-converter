version: '3.3'

services:
  image-converter:
    build: ./
    command: ./image-converter
    ports:
      - "8000:8000"
    depends_on:
      - postgresql

  postgresql:
    restart: always
    image: postgres:latest
    container_name: postgresql
    hostname: "postgres.local"
    ports:
      - "5438:5432"
    volumes:
      - ./scripts/sql.sql:/docker-entrypoint-initdb.d/sql.sql
      - ./data:/var/lib/postgresql/data/
